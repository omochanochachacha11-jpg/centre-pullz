---
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
}
const { title, description = "Weekly crane-game weight-distribution reports, translated." } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <header class="site-header">
      <div class="wrap site-header__inner">
        <a href="/" class="brand">
          <span class="brand__mark mono">⌖</span>
          <span class="display">CENTER PULL</span>
        </a>
        <nav class="site-nav mono">
          <a href="/"><span class="en">This Week</span><span class="zh-inline">本周</span></a>
          <a href="/database"><span class="en">Database</span><span class="zh-inline">数据库</span></a>
          <a href="https://smacre.jp/" target="_blank" rel="noopener">Source: smacre.jp ↗</a>
        </nav>
      </div>
    </header>
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <div class="wrap mono">
        <p>
          Weight-distribution reports are translated and summarized from
          <a href="https://smacre.jp/" target="_blank" rel="noopener">smacre.jp</a>. Original Japanese text, photo
          credit, and source links are kept on every entry. Updated every 3 days.
        </p>
        <p class="footer-zh">
          重心数据翻译整理自 <a href="https://smacre.jp/" target="_blank" rel="noopener">smacre.jp</a>，
          每条记录均保留日文原文、图片来源及原文链接。每 3 天更新一次。
        </p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  .site-header {
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    background: var(--bg);
    z-index: 10;
  }
  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 8px;
    text-decoration: none;
    color: #fff;
    font-size: 18px;
    letter-spacing: -0.01em;
  }
  .brand__mark {
    color: var(--accent);
    font-size: 20px;
  }
  .site-nav {
    display: flex;
    gap: 24px;
    font-size: 13px;
  }
  .site-nav a {
    text-decoration: none;
    color: var(--text-muted);
    display: flex;
    flex-direction: column;
    gap: 1px;
    line-height: 1.2;
  }
  .site-nav a:hover {
    color: var(--accent);
  }
  .site-nav .zh-inline {
    font-size: 10px;
    color: var(--text-muted);
  }
  .site-footer {
    border-top: 1px solid var(--border);
    margin-top: 64px;
    padding: 24px 0 48px;
    color: var(--text-muted);
    font-size: 12px;
  }
  .site-footer a {
    color: var(--accent);
  }
  .footer-zh {
    color: var(--text);
    margin-top: 6px;
  }
</style>
